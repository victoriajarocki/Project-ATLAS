# Select the active model provider.
# Available values: mock, openai
ATLAS_PROVIDER=mock

# Model used when the OpenAI provider is selected.
ATLAS_MODEL=gpt-5.5

# Never commit a real API key.
OPENAI_API_KEY=